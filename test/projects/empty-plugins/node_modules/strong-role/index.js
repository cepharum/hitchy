"use strict";

module.exports = function( options ) {
	const that = this; // eslint-disable-line no-unused-vars

	return {
		// claim to fill some role dynamically (to supersede weak-role by default)
		$meta: {
			role: options.strongRole || "important"
		},
	};
};
